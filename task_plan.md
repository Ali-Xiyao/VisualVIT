# Task Plan: 两份 VisualVIT Proposal 融合与正式实验落地

## Authoritative Update — 2026-07-26 R25.1 Semantic Repair

用户提供的审计文本已被采纳为新的执行依据，正式规范见
`docs/R25_1_ERRATUM.md`。历史 `dd9c242` 只保留为 R25 Session 5
dry-run，禁止将 `three_event_macro_f1` 或 B4 matching contrast 解释为
Stable/Improved/Worse 结果。当前分支为 `r25.1-semantic-repair`。

当前 survival-gate 顺序：

- [x] S0a：建立 R25.1 分支，历史基线不改写
- [x] S0b：写入 R25.1 勘误、术语、结论边界与 R26 门控
- [x] S1a：把 matching event 与 progression label 指标命名空间彻底拆开
- [x] S1b：修复 R25 Q4，使 matcher qualification 不再伪装为 progression 评估
- [x] S1c：增加 progression target 真实进入评估的契约与回归测试
- [x] S1d：focused tests、ruff、全量 pytest 通过
- [x] S2：构建 pair-level matching manifest 与 entity-level progression manifest
- [ ] S3：冻结 R25.1 matching protocol，在空闲 GPU1 顺序运行 process A/B
  - [x] Process A：`AWAITING_FRESH_PROCESS_REPRODUCTION`
  - [ ] Process B：GPU1 watcher PID 27888 active; waits for two idle samples
- [ ] S4：独立验证 A/B 并发布 matching qualification certificate
- [ ] C1：运行真正的 oracle-correct vs deranged progression classifier
  - [x] Draft protocol written; status `DRAFT_AWAITING_R25_1_Q6`
  - [ ] Pin R25.1 certificate/feature hashes and freeze
  - [ ] Implement and verify runner
    - [x] Representation, B4 isomorphism, folds, head, bootstrap, and gates implemented
    - [x] Focused verification: 9 passed, ruff clean
  - [ ] Run only after S4 is green
- [ ] C2：仅 C1 通过后训练 learned matcher
- [ ] VLM/DIVE：仅 C1、C2、VLM transfer 依次通过后解锁

当前硬停规则：禁止 RAD-DINO rescue、learned matcher、frozen VLM、
DIVE Phase II 和扩大 GPU 实验，直到前置 gate 明确为 green。

S1/S2 验证结果：focused tests `49 passed`；全量
`483 passed, 1 xfailed`；ruff 与 compileall 通过。新鲜 manifest 为
189 patients / 189 pairs / 793 entities，Stable 371 / Improved 160 /
Worse 262。下一阶段是先冻结 R26 structured-classifier 协议与最小
pipeline；C1 运行前仍需完成 head、split、seed、bootstrap 与正式 test
封存细节，当前未启动训练。

2026-07-26 续跑现场复核：VisualVIT 无活动实验；GPU0 上存在外部
`n5_rebind_tf_eval.py` smoke（约 9.3 GiB），不得停止或竞争；GPU1 为
0 MiB / 0% 空闲。S3/S4 优先于 C1，因为 R26 必须消费新鲜、已复现的
R25.1 特征与 matching 资格证书。

## Goal
将 `CAPES_Final_Complete_Proposal_CN.md` 与 `DIVE_Proposal.md` 融合为一套研究问题清晰、资源可获得、代码可复现、实验可审计、结果可用于正式论文写作的完整执行方案，并在用户批准设计后按门槛顺序完成全部正式实验。

## Next Step
R25 protocol spec three-label rewrite COMPLETE (`docs/superpowers/specs/2026-07-25-chest-imagenome-real-data-protocol-v1.md`, 467 lines, PRE_FREEZE). All 5 open design questions resolved by pre-audit. Three-label persistent endpoint (Stable/Improved/Worse) is frozen as primary; five-label dropped (infeasible: Chest ImaGenome attribute files cover only previous images). Pre-audit confirmed 189 patients / 795 persistent rows (margin +89 over Q7 threshold). **NEXT: implement the R25 qualification runner** as a new namespace (not modifying R24 frozen source), align the 2 R25-bound pytest fixes (`test_query_anchor_r4_runner.py:274` dry_run_authorized + r16 bundle regeneration via R16 finalizer), then freeze R25 protocol/registry and run 3 independent Gate-0 processes. Target: 444/444 pytest before R25 freeze.

## Current Phase
**Authoritative update (2026-07-25 session 4): R25 protocol spec three-label rewrite COMPLETE.** Full consistency audit passed (467 lines, all five-label/birth/death references are in infeasibility-explanation context only). 15 replacements applied via Python scripts to bypass the Edit tool cache issue. The spec now reflects: (1) three-label persistent endpoint as primary (Stable/Improved/Worse), matching R24 v3; (2) five-label dropped as infeasible (Chest ImaGenome attribute files cover only previous images, zero current images); (3) pre-audit cohort counts frozen (189 patients / 189 pairs / 795 persistent rows, margin +89 over Q7 threshold); (4) all 5 open design questions resolved with frozen decisions. pytest is 442/444 (verified by fresh rerun); both remaining failures are R25-bound and will be fixed alongside the R25 implementation (test_query_anchor_r4_runner.py:274 dry_run_authorized + r16 bundle regeneration). **NEXT: implement R25 qualification runner as new namespace, align 2 pytest fixes, freeze R25, run 3 Gate-0.**

**Authoritative update (2026-07-25 session 3): pytest recovery complete as far as R24 allows — 442/444 passing. — 442/444 passing.** Reconstructed `.tmp/audit_r11_registered.py` (3,410 B, fixes failures #4/#5) and `.tmp/audit_r24_registered.py` (12,001 B, fixes failure #3) by reverse-engineering the materializer contracts from the R11/R24 registries; these are engineering test fixtures not in the R24 source manifest, so the R24 freeze record is intact. The 2 remaining failures are both genuinely R25-bound and cannot be worked around without violating evidence-integrity: (1) r16 frozen-validator bundle — its value is byte-frozen R14-era source (22 files, SHA-256 pinned in `required_file_sha256`); those bytes are unrecoverable (`.git` dir empty, no backup, not embedded in the 97 KB R14 protocol md), so an inauthentic rebuild would defeat the test's "frozen bytes, never live R24" purpose — must be regenerated by the R16 finalizer under R25; (2) `dry_run_authorized` truthy-vs-`is False` — both `test_query_anchor_r4_runner.py:274` and `run_query_anchor_r4.py:2466-2467` are hashed in the R24 freeze record, so the alignment fix + re-freeze is an R25 action. **Option 2 (fix pytest) is now as complete as R24 authority permits. Proceeding to option 1 (R25+ real-data protocol design):** refine the PRE_FREEZE candidate at `docs/superpowers/specs/2026-07-25-chest-imagenome-real-data-protocol-v1.md`, resolve its 5 open design questions, and prepare it for freeze. R25 will carry the two pytest fixes + re-freeze alongside the real-data cohort qualification.

**Authoritative update (2026-07-25 session 2): User directive "先2 再1" calibrated against planning-with-files skill + existing goal docs.** Priority order is now: **(2) FIRST fix the 5 failed pytest** by reconstructing the lost `.tmp/` external-materializer scripts (`audit_r11_registered.py`, `audit_r24_registered.py`, `r16_frozen_r14_validation_bundle.py` + `r16_frozen_r14_validator_bundle_v5/`), with failure #2 (`dry_run_authorized` truthy-vs-`is False` mismatch in `test_query_anchor_r4_runner.py:274`) bundled into R25 re-freeze since editing that test file breaks the R24 freeze-record hash; **THEN (1) design R25+ real-data protocol** that joins Chest ImaGenome `gold_dataset/` per-bbox annotations to R24-qualified MIMIC-CXR metadata → enlarge the underpowered B4 cohort (current 22-patient strict persistent endpoint gave +7.25 pp CI `[-0.47, +16.92]`). R25 protocol spec to be written at `docs/superpowers/specs/2026-07-25-chest-imagenome-real-data-protocol-v1.md` following the frozen v3 pattern. Chest ImaGenome gold structure confirmed: `gold_object_comparison_with_coordinates.txt` (3,921 rows, `comparison` ∈ {no change, improved, worsened} → stable/improved/worse) + `gold_object_attribute_with_coordinates.txt` (15,688 rows, per-image entity presence → derive new/resolved); both 224×224 AND original coords already in the gold file; `silver_dataset/splits/{train,valid,test,images_to_avoid}.csv` provide patient-disjoint MIMIC splits with `dicom_id` for the join. Boundary unchanged: formal test sealed, formal entity-level claim locked, allocation `4161` retained and unused until R25 cohort qualifies and a larger identifiable persistent-entity subset passes the B4 survival gate.

**Authoritative update (2026-07-25): Workspace cleanup and Chest ImaGenome dataset ingestion complete. All R1–R23 frozen artifacts and protocol markdown files preserved in place per user instruction. Pure debug clutter removed: `.tmp/` (560 MB, 445 dirs), `tmp/`, `.pytest_cache/`, `.ruff_cache/`, and 30+ `.pytest_tmp_*` / `pytest-cache-files-*` root directories — total ~570 MB freed. Source code, tests, scripts, R24 frozen source manifest, and all R-frozen protocol files untouched; `python -m compileall` passes (exit 0). **pytest: 439 passed, 5 failed** — the 5 failures are all caused by the `.tmp/` deletion also removing registered external-materializer scripts (`audit_r11_registered.py`, `audit_r24_registered.py`, `r16_frozen_r14_validation_bundle.py`) that lived at the top level of `.tmp/` alongside the pytest basetemp clutter; R24 freeze itself is intact (those scripts are `external_materializers`, not in the source manifest), and all R24 scientific artifacts are preserved. See `progress.md` § 2026-07-25 erratum for the full impact assessment and recovery options. Chest ImaGenome v1.0.0 zip (`H:\2018b\chest-imagenome-dataset-1.0.0.zip`, SHA-256 `D5D29237…E3BB033`, 1.55 GB) extracted to `F:\VisualVIT_runtime\050_routeC\data\chest_imagenome\chest-imagenome-dataset-1.0.0\` (5.99 GB, 57 entries, all verified against in-package `SHA256SUMS.txt`: 57 OK / 0 mismatch / 0 missing). Backup copy placed at `H:\Xiyao_Wang\000_Public Dataset\chest-imagenome-dataset-1.0.0.zip` with `.sha256` sidecar (SHA-256 matches source). DATASHEET written to `F:\VisualVIT_runtime\050_routeC\data\chest_imagenome\DATASHEET.md`. Dataset is **PhysioNet Credentialed Health Data License 1.5.0** (not CC BY) — annotations only, parent MIMIC-CXR-JPG images require separate credentialed access. Next formal run: build the R25+ real-data protocol that joins Chest ImaGenome `gold_dataset/` per-bbox annotations to the local MIMIC-CXR metadata already qualified in R24 `real_qualification` v3, applying the per-image `gold_bbox_scaling_factors_original_to_224x224.csv` transform, to enlarge the underpowered B4 cohort (current 22-patient strict persistent endpoint gave +7.25 pp CI `[-0.47, +16.92]`).**

**Authoritative update (2026-07-24): Phase 3 real-data survival evidence is terminally green, and the first registered real-data secondary experiment has passed independent result audit. The local mathematical machinery is sufficient for exact two-sided-null transport, allocation, tokenization, patient-disjoint evaluation, crossed B4 derangement, and hierarchical inference. The remaining blocker is scientific rather than computational: the strict identity-specific B4 contrast is underpowered and its 95% interval crosses zero. Therefore the five-label structural result is retained as a non-confirmatory upper bound; formal entity-level and clinical claims remain locked. The next formal run must increase identifiable persistent-entity support before VLM scaling.**

**Authoritative update (2026-07-23): Phase 3H, R12 dry-run mode-dispatch correction (`in_progress`). R11 was successfully frozen and independently Gate-0 verified, but its first dry-run command fail-closed before output-root creation: the main entrypoint incorrectly sent `dry_run` through an R11 guard whose intentionally closed authorization set permits only `independent_reproduction`. This is an immutable R11 technical-negative artifact, not a method result. R12 must preserve R11 scientific contracts and native authorization safety while bypassing certificate consumption only for the already-freeze-authorized dry-run mode; it must re-freeze and audit before a new dry-run. Formal data/test remain HOLD/SEALED and Slurm 4161 remains unused.**

Phase 3D：R6 冻结前证据链硬化与 staged-flow 校准（in_progress）；fresh dry-run 尚未授权，真实 train/dev 主实验仍受机制门和数据资格门锁定

## Scope Boundary
- 用户于 2026-07-19 将目标扩展为方法学论文级闭环：持续查新/校准方法，直接获取合法数据与权重，在保留分配 `4161 / tpami / gpu01` 上跑通测试实验、主实验和关键消融，直到找到可辩护主方法。
- 2026-07-22 注册 R3 已以 `STOP_LEARNED_RECOVERY` 结束并永久保留：结构、working oracle、独立能力正控、边际控制与 B4 绑定通过，但 learned matcher 的跨 split 身份恢复失败；禁止通过删 seed、降阈值或同协议调参改写该结论。
- R4 只允许方法级修正：query-independent two-sided-null partial transport、旋转不变 pair utility、query 仅在 transport 后门控 mediator，并把 matcher 资格训练与 readout 共适应解耦。Sinkhorn/Hungarian 是求解器或基线，不得包装成 novelty。
- 用户随后明确收紧并行边界：终止全部子智能体，此后任一时刻最多同时开启 4 个子智能体；禁止子智能体自行扩散出超额并发。
- 新授权允许实现端到端方法、下载公开且许可兼容的资产并启动服务器实验；它不取消 license/DUA/伦理、数据污染、一次性 test reveal、统计 power 和证据审计门槛。
- 远程目标暂记为 `sues-hpc` Slurm job `4161`（历史请求：1 GPU、4 CPU、64 GiB、gpu01、长驻 Bash）；每次使用前必须以 live `squeue/scontrol/srun nvidia-smi` 复核。禁止 `scancel 4161`、释放或替换该分配，除非用户明确改变指令。
- 用户已于 2026-07-13 确认路线 C 架构并授权先跑预实验；当前允许实现和运行明确标为 `NON_CONFIRMATORY_PROXY` 的资格预实验。
- 正式 test、正式 B4 因果实验、需要受限许可的父图像与任何论文主张仍保持封存，直到全部 formal gates 通过。
- 当前 verdict 为 `GO_CAPES_CI_V1_S010_S070_SURVIVAL + NO_GO_REAL_PILOT_UNTIL_D010_D020`；真实 Qwen3-VL exact-64 已通过，但 gold-data/lineage 与真实 B4/Recovery 仍未证明。
- `H:\Xiyao_Wang` 是模型、数据集、既有代码和潜在实验资源的主要盘点范围。
- 两份原 proposal 保持原样；融合方案另建权威设计/实验手册，除非用户明确要求改写原文。
- 红队判定：当前仅允许数据/工程资格审计；在 oracle/许可、B4 同构、null matching、防污染、统计预注册、classifier→VLM transfer 设计六项闭合前，正式机制实验为 NO-GO。

## Phases

### Phase 0: Requirements, Proposal Synthesis, and Resource Discovery
- [x] 深读并结构化对照两份 proposal：研究问题、假设、方法、数据、指标、实验表、风险
- [x] 只读盘点 `H:\Xiyao_Wang` 中可复用的数据集、模型、代码、环境与存储条件
- [x] 确认总体路线：路线 C，总计划两阶段，第一阶段执行 CAPES-first + DIVE-soft
- [x] 确认架构；其余数据/许可、实验/统计与执行边界按保守门槛写入书面规格
- [x] 提出 2–3 种融合路径，给出取舍和推荐方案
- [x] 取得用户对路线 C 架构及预实验启动的确认（2026-07-13）
- **Status:** complete

### Phase 1: Approved Design Spec and Formal Experiment Manual
- [x] 把获批设计写入 `docs/superpowers/specs/2026-07-13-visualvit-unified-research-design.md`
- [x] 自检占位符、内部矛盾、歧义和范围
- [x] 用户确认路线 C 架构并明确授权启动预实验；书面规格不得扩大该授权到正式实验
- [x] 生成逐任务预实验实施计划与 claim-driven 实验手册（精确到配置、产物和验收门槛）
- [x] 定义实验 tracker、versioned/fixed plan、manifest 和失败记录格式
- **Status:** complete

### Phase 2: Environment, Data, and Model Qualification
- [x] 完成预实验算力、磁盘、依赖、已有资产与下载来源检查；正式许可/伦理仍未闭合
- [x] 完成 2024–2026 最新相关方法与 ICLR/CVPR/AAAI 评价要求查新，形成可辩护 novelty delta
- [x] 实时确认 `4161/tpami/gpu01` 状态、GPU/CPU/内存、共享项目路径、远程环境和长驻启动方式
- [x] 冻结主方法候选、最小强基线、关键消融与 survival-gate 顺序
- [ ] 对数据进行版本固定、校验和、划分冻结、泄漏检查与数据卡记录
- [x] 完成本地 BiomedCLIP/Qwen 最小 smoke、主要权重/index hash 与 Qwen 全部分片 hash；正式 RAD-DINO 后置
- [x] 建立预实验配置、日志、run summaries、tracker、环境快照与统一预实验证据 manifest；正式隔离 env/Git 与正式数据 manifest 仍未闭合
- **Status:** in_progress

### Phase 3: Survival Gates and Pilot Validation
- [x] 实现 sub-stochastic two-sided-null MatchGraph、deterministic global allocator 与完整 64-token contract
- [x] 实现 MatchGraph → projector/position/attention → frozen VLM 的真实注入路径
- [x] Gate 1（本地 survival）：schema、标签顺序、mass/allocator/metric 合约和 76 项回归测试通过
- [x] Gate 2（本地 survival）：30-case 五类过拟合、梯度隔离、干预敏感性和双进程精确复现通过
- [ ] Gate 3：R4 在 analytic-clean 与 anti-equivalence challenge 两个 stratum 中同时通过身份恢复、下游效应、基线优势与独立进程复现
- [x] Gate 4：4161 CPU/GPU 回归与真实 Qwen3-VL exact-64 流程可重跑且结果一致
- [ ] 任一门槛失败时停止下游扩展，先诊断并形成证据
- **Status:** in_progress

### Phase 4: Formal Experiments
- [ ] 锁定训练预算、数据划分、调参协议、随机种子与评估脚本
- [ ] 完成主结果、强基线、关键消融、敏感性/鲁棒性、效率与失败案例分析
- [ ] 独立实验按可用 GPU/主机并行，但禁止共享写路径和测试集反馈调参
- [ ] 关键结论至少多随机种子复现，报告均值、标准差/置信区间和适当显著性检验
- **Status:** pending

### Phase 5: Audit, Reproduction, and Research Handoff
- [ ] 从最终 manifest 重建全部表格和图，不手工抄录结果
- [ ] 独立复跑关键配置并核对 checkpoint、日志、配置、环境和指标证据链
- [ ] 写回权威实验手册、结果索引、限制与负结果
- [ ] 执行最终需求审计并给出完成/门槛阻塞的明确结论
- **Status:** pending

## 2026-07-23 Current execution authority: R12 closed / R13 required

- [x] Freeze R12 and independently rerun Gate-0: final protocol `134cff9e7ff3c0353583cbdc43ef74e9bc515148c1e3bee2bc413dad546392ed`; registry `443569184f3cf3f465d7da71bbe5bb70462733e45d883959769b5dede0e549eb`; staged plus three independent checks were `74/74`.
- [x] Execute the one registered R12 CPU dry-run: return code `0`, status `DRY_RUN_VALIDATED_R12`, no training or formal-data access.
- [ ] Complete the independent dry-run audit. It is currently technically blocked before evidence publication because R12 froze no `dryrun_postrun_auditor` materializer binding. Preserve R12 unchanged; do not issue a smoke certificate by any workaround.
- [x] Build and close R13/R14 authorization authority: R14 freezes both external auditor identities and completes fresh dry-run, dry-run audit, seed-17 smoke, and smoke audit under isolated roots.
- [ ] Run the only R14 certificate-authorized CPU registered-local preexperiment: seeds `[17,29,43]`, 500 steps, then inspect its strict summary before preparing a separate reproduction authority. Keep 4161 locked.

### R14 registered local result

- [x] CPU registered preexperiment completed under the R14 one-shot certificate: three seeds `[17,29,43]`, 500 steps, strict-valid `PASS_R14_GATES_0_TO_7_AWAITING_INDEPENDENT_REPRODUCTION`, summary SHA `bdf1b4609593dda3833ab1e06489d50fddc0e7085d254b42a0d82a789491b8cb`.
- [ ] Build a separate frozen reproduction authority and obtain independent local reproduction before considering any 4161 action. Keep formal data, downloads, and 4161 locked.

### R15 immutable audit boundary

- [x] Freeze R15 and preserve its failed R14 registered-summary audit. It issued no reproduction certificate/claim/root; failure is a self-containment defect in the frozen validation snapshot.
- [ ] Build R16 with a fully self-contained R14 runner/semantic-validation dependency bundle, then issue fresh two-child local-reproduction credentials only after its independent R14 audit passes.

### Methodology eligibility boundary

- [x] Red-team the proposed novelty against current OT and longitudinal CXR work; paper claim is constrained to identity-binding mechanism evidence, not OT/prompting novelty.
- [ ] After independent reproduction, perform official real-data qualification and a B4a/B4b identifiability/leakage smoke before any dataset/model download or 4161 GPU run.

## Formal Experiment Standards (Locked design; execution remains gate-blocked)
1. 数据：来源、版本、许可证、校验和、预处理、划分和泄漏检查全部落盘。
2. 模型：架构/权重版本、许可证、校验和、精度模式和预处理严格固定。
3. 比较：同数据划分、同评估协议、可比训练预算；调参范围事先登记。
4. 统计：固定种子列表；关键结果多种子；报告单次值、均值、离散度和置信区间。
5. 可复现：保存命令、配置、代码状态、依赖/硬件快照、日志、checkpoint 和结果 manifest。
6. 审计：失败运行不覆盖；测试集不参与模型选择；所有表图由结构化结果自动生成。
7. 论文证据保持紧凑：最多 2 个主张、首轮 4 个核心实验块、3 个强基线家族；每个 run 必须对应 claim、success criterion、failure interpretation 和表/图目标。
8. 默认对随机性敏感的关键比较运行 3 个预注册种子；若 pilot 方差显示 3 个种子不足，则在正式手册中基于效应量/方差上调，而不是事后按显著性追加。

## Key Questions
1. [已回答] 用户于 2026-07-11 批准路线 C：总计划分两阶段，第一阶段按路线 A 做 CAPES-first + DIVE-lite，只有正式门槛通过后才解锁通用 DIVE/HPC。
2. [已回答] H 盘具备 MIMIC/CheXpert、BiomedCLIP/DINO/完整 Qwen2-VL 与 VIVID/PJP/024_mm 骨架；缺正式 oracle annotations、RAD-DINO/BioViL-T、DIVE benchmarks/训练集。
3. [已回答] 本地为 2×RTX 3090 24 GiB；远程 `4161` 已于 2026-07-19 live 确认为 RUNNING，分配 A800 80GB、4 CPU、64 GiB，gpu01 节点可用。
4. [已回答] 目标为 ICLR/CVPR/AAAI 水平的方法学文章；必须具备测试实验、主实验、强基线、关键消融和正式统计证据，不能以组件 smoke 代替。
5. [已回答] job `4161` 已从历史 Priority pending 转为 RUNNING；建议新建共享项目根 `~/projects/xiyaowang/050_VisualVIT`。现有服务器 MIMIC-CXR 和 BiomedCLIP 可复用，Qwen2-VL/RAD-DINO/Chest ImaGenome/CheXTemporal 尚未发现。

## Decisions Made
| Decision | Rationale |
|---|---|
| 先只读调研和设计，获批后再执行 | 用户明确要求先计划；brainstorming 要求设计审批门槛 |
| 使用 `task_plan.md`、`findings.md`、`progress.md` 持久化状态 | 保证长周期实验可恢复、可审计 |
| 机制门槛优先于全量扩展 | 避免在核心机制未成立时浪费正式算力 |
| 当前目录没有 Git 仓库，暂不提交 | `git` 检查确认无 `.git`；后续由用户决定是否初始化或放入现有仓库 |
| H 保持只读，下载/运行写 F | 2026-07-13 新快照显示 H 空闲约 839.3 GiB，旧 6.15 GiB 快照已过时；只读策略保留是为保护既有资产与 provenance，不再以容量不足为理由 |
| 确定 CAPES-first、DIVE 收缩为 learned relation module/强基线 | DIVE finding novelty 已被直接近邻覆盖，完整数据/算力缺失；CAPES 的受控 binding 因果边界仍可能成立且本地工程复用度更高；路线 C 已获用户确认 |
| 采用路线 C | 先完成路线 A 的可因果识别医学闭环，再条件性泛化为 DIVE；同时保留两份 proposal 的价值并隔离通用路线失败风险 |
| 路线 C 已获用户批准（2026-07-11） | 进入分节设计确认；书面 spec 经用户复核后才生成 implementation plan、下载或运行实验 |
| 用户确认架构并授权预实验（2026-07-13） | 先运行 `NON_CONFIRMATORY_PROXY` 资格预实验并回报结果；正式测试集与正式主张继续受六项门槛约束 |
| 运行时根已启用为 `F:\VisualVIT_runtime\050_routeC`，代码/规格留在当前 E 工作区 | 当前 F 空闲约 213.75 GiB；继续设置 180 GiB 项目硬上限并始终保留至少 60 GiB；H 全面禁写 |
| 方法论文阶段使用保留分配 `4161/tpami/gpu01` | 用户明确指定 GPU+CPU 服务器并要求跑完不释放；只取消子 step，不取消父 allocation |
| 先查新并冻结主方法，再扩到主实验 | 当前缺 soft/global allocator 和真实 VLM 注入；直接扩大旧 CLS proxy 不能回答方法主张 |
| 后续同时最多 4 个子智能体，且禁止其下级派生 | 用户于 2026-07-19 明确要求；当前使用滚动并行，活跃子智能体始终不超过 4，完成一条后才复用该槽位 |
| 机制 pilot 至少 3 个训练种子 | 单训练种子无法估计 seed variance；先用有序 seed bank 前 3 个，仅按预注册功效规则扩至前 5 个 |
| S075 失败后先修复 B4 可识别性，不启用 learned rescue | 三 seed B4 denominator 未在每 seed 为正且 mean Delta_bind 仅 +2.6349 pp；single rescue 的前提不成立，不能用调参掩盖 anchor/task 缺陷 |

## Errors Encountered
| Error | Attempt | Resolution |
|---|---:|---|
| Reproduction verifier was first given `summary.json` paths although it expects run directories, producing a duplicated `summary.json\\summary.json` lookup | 1 | Re-ran with the two run directories; exact registered-field reproduction passed |
| Newly spawned subagents could not refresh a revoked access token | 4 | No work product was accepted from those failed turns; continued safely in the main agent and kept concurrency below four |
| 在非 Git 目录直接运行 `git status`/`git log` 返回 “not a git repository” | 1 | 改为先检测 `.git`；记录当前目录没有 Git 仓库，不重复裸调用 |
| 尝试在主代理 + 3 个运行中子代理之外再启动第 4 个子代理，平台返回 `agent thread limit reached` | 1 | 采用滚动并行；任一代理结束即换入下一独立任务，保持并发槽满载 |
| 更新规划文件时用不存在的 progress 尾部锚点，`apply_patch` 校验失败 | 1 | 先读取实际文件尾部，再用准确锚点重试；失败调用未产生文件修改 |
| 更新当前计划的复合补丁因错误日志锚点不精确而校验失败 | 1 | 读取实际片段后拆分并使用精确锚点；失败调用未修改文件 |
| `experiment-plan` 指向的三个 `shared-references/output-*.md` 在本机缺失 | 1 | 不重复使用错误路径；采用保守等价协议：时间戳文件、固定入口文件与 `MANIFEST.md` 同步维护 |
| 占位符扫描使用了 ripgrep 默认引擎不支持的 look-around | 1 | 改用 `rg --pcre2`；复扫结果为 `NO_PLACEHOLDER_HITS`，且固定/时间戳文件 SHA-256 一致 |
| Qwen2-VL 2B 首次 smoke 在 CUDA context 初始化前调用 `reset_peak_memory_stats(cuda:1)`，Windows PyTorch 返回 `Invalid device argument` | 1 | 在统计前显式 `torch.cuda.set_device` 并分配 1-element tensor 初始化 context；首轮未加载模型且无结果 claim |
| Qwen2-VL 2B/7B 均完成双图生成但返回精确裸标签 `improved`，未遵守要求的 `ANSWER:` 前缀 | 1 | 保留两次 schema FAIL；新增无默认回退的严格 adapter 并完成 2B/7B 复跑 PASS；该 smoke 仍不含 64-token 注入 |
| 终审发现 fractional transport 会被 hard tokenizer 静默误编码 | 1 | hard tokenizer 改为显式拒绝 fractional plan，新增 adversarial test；soft allocator 未实现，正式路径保持 NO-GO |
| 终审发现 synthetic birth false positives 会被 oracle filter 丢弃 | 1 | 改为预测 birth set 与 gold 不一致即报错，不再选择性过滤；修复后 matcher=1.0 的三 seed 结果重跑 |
| 终审发现 B4 audit 未比较独立输入/实际训练 contract | 1 | 比较两份 RegionBatch 全字段 hash，并审计实际初始化、optimizer/steps/seed/preprocessing；新增输入突变负向测试 |
| MIMIC raw +4.29 pp 被未收敛 seed43 deranged 条件驱动 | 1 | 新增 train-F1/loss convergence gate；重跑明确返回 `FAIL_CONVERGENCE_GATE`，aggregate 标为不可解释 |
| `novelty-check` 要求的 `mcp__codex__codex` reviewer tool 与 review-tracing reference 在本环境不可用 | 1 | 不伪造跨模型审查；改用独立高推理 subagent + 主代理一手来源复核，并将等价 trace 写入 `.aris/traces/novelty-check/` |
| OpenReview full PDF 被 browser challenge 拦截，ar5iv HTML 重定向被安全策略拒绝 | 1 | 保留已取得的 OpenReview/arXiv 摘要证据；后续改用 arXiv PDF/source、官方 GitHub 与可访问 conference PDF，不重复同一 OpenReview challenge |
| S070 首次真实 Qwen3-VL smoke 将 FP32 synthetic relation features 输入 BF16 projector，触发 dtype mismatch | 1 | fail-closed 保留失败 JSON/log；显式按 projector dtype 构造 feature/metadata，重建新 SHA payload，以同 seed 双进程重跑并逐字段复现 PASS |
| 最终 live 状态命令在 login node 追加 `nvidia-smi`，该节点无此命令而返回非零 | 1 | 不启动无必要的新 GPU step；使用已保存的 compute-step A800 证据，并单独重跑 `squeue/squeue --steps` 成功确认 4161 RUNNING、仅 batch step |
| 首次以 `fork_turns=all` 同时指定 worker agent type，协作接口拒绝该组合 | 1 | 未创建子智能体；改为 `fork_turns=2` + 明确 worker/explorer role，随后严格创建 4 个并禁止下级派生 |
| PowerShell here-string 经管道传 Python 时中文 `附加文件` 路径被替换为 `????`，`np.load` 返回 Invalid argument | 1 | 不重复管道内嵌路径；由 PowerShell 解析 Unicode 路径并作为 argv 传 Python，mmap shape/dtype 与 JSON schema 检查成功 |
| `squeue --steps` 误用了该集群 step formatter 不支持的状态占位符 `%T`/`%t` | 2 | 先用 `squeue --steps --helpformat` 核对可用字段，再改用默认 `squeue --steps -j 4161`；成功确认仅 `4161.batch`，不再猜测状态占位符 |
| Windows Python 3.12 `tempfile.mkdtemp()`/pytest `tmp_path` 以 mode 0700 创建目录后当前执行 token 立即遭遇 WinError 5 | 3 | 最小探针确认普通 `Path.mkdir()` 正常；数据资格测试改用文件内 workspace-safe 唯一目录 fixture，并关闭 pytest cache，不把 ACL 故障误判成产品逻辑失败 |
| 合成校准 runner 首稿只训练 learned-soft 后做 B4a/B4b 推理替换，并让公平基线复用 learned null-aware cost | 1 | 首稿未运行正式三 seed 结果；主线程审查后要求从同一初始状态分别完整训练 B4a/B4b/learned，并把 Hungarian/Sinkhorn 改为共同的 oracle-free cosine cost，controlled rows 与 inference interventions 分栏 |
| 重构后的校准测试在标准 `PYTHONPATH=src` 命令下无法导入 namespace `scripts.run_synthetic_calibration_grid` | 1 | 不新增全局包；仅在该测试文件显式把 workspace 根加入 `sys.path`，随后必须用与全套一致的命令 fresh 复测，旧 4-pass 不采信 |
| 对整个历史 `scripts/` 运行 Ruff format/lint 时发现 5 个旧脚本待格式化、21 个旧 E402 | 1 | 未机械改写不在本轮范围的历史脚本；本轮新增/修改文件单独 Ruff/format/compile 全绿，完整 pytest 126/126 PASS，并把全仓历史债务与本轮回归结论分开记录 |

## S075/S076 Calibration Gate Update
- S075 registered anchor：`[17,29,43] × 80 steps` 技术执行完成，但机制门 `FAIL`。Delta_bind=`+8.9524/-4.6190/+3.5714 pp`，mean=`+2.6349 pp`；Recovery 不资格化且偏低；A1/A2 方向不稳定。
- S076 fresh-process reproduction：仅忽略 `walltime_seconds` 后全部注册字段完全一致，mismatch=`0`，canonical SHA256=`a8117be52f57e18b09eacbd6b394575c3a9047b6cb533bacf8ad194779811079`。
- 当前 gate：`S080/F100/F200` 继续 LOCKED；不允许正式 test reveal、宽基线/消融扩展或 learned single rescue。下一步仅允许 B4 task identifiability、训练动态和干预语义的单变量诊断。
- 4161 live 状态仍为 `RUNNING / gpu01 / 4 CPU / 64 GiB / 1 GPU`，仅 `4161.batch`；父 allocation 未释放。

## 2026-07-22 R2c/R3/R4 Calibration Checkpoint
- R2c 服务器注册运行（Slurm step `4161.4552`）以 `STOP_MARGINAL_CONTROL` 结束；原因是 prior-only 控制在本来不可辨识的数据上被错误要求达到 train F1 `>=0.90`。该协议缺陷不等于方法失败，结果保持 immutable negative。
- R3 把控制能力验证移到独立可分正控；三 seed 全部 train/dev F1=`1.0`、finite-gradient 500/500、derangement F1=`0`，正式 marginal bypass controls 全部 development F1 `<=0.45`。
- R3 本地注册结果 `artifacts/calibration/capes_ci_query_v2_r3_registered_local_20260722/summary.json`：working oracle PASS、marginal control PASS、persistent binding PASS（每 seed `+83.33 pp`），但 learned recovery FAIL：hard identity=`0.1389/0.0139/0.1944`，aggregate=`0.1157`；soft oracle mass aggregate=`0.0987`；persistent macro-F1=`0.2239/0.1942/0.3285`。解析 Hungarian/Sinkhorn 在相同 splits 分别为 hard=`1.0`、soft mass=`0.9425`。
- R3 失败归因于 downstream CE 与 identity gate 不一致、可训练 readout 吸收任务损失，以及不具旋转不变性的自由双线性投影。R4 不允许只改学习率/步数/温度；必须采用新的协议版本和新 artifact 路径。
- R4 当前准入顺序：冻结协议与 challenge DGP -> 原子测试 -> fresh dry-run -> one-step smoke -> 本地三种子 registered -> 仅在本地全部门通过后同步到 `4161` 做服务器 registered 与双独立进程复现。
- 真实主实验和消融仍保持 HOLD：现有 CheXTemporal 发布粒度无法提供无歧义 per-entity five-label oracle；需要合法的 MS-CXR-T/Chest ImaGenome/ReXGradient 或经过校正的 per-box temporal annotation 与完整 lineage/DUA 证据。
- 2026-07-22 fresh live audit：`4161|tpami|RUNNING|gpu01|4|64G|gres/gpu:1`，EndTime=`2027-07-17T23:27:57`；父 allocation 继续保留，禁止 `scancel 4161`。

## 2026-07-22 R5 preregistration reset

- **Current authority:** Phase 3D, R5 method calibration (`in_progress`). R4 dry-run and smoke artifacts are immutable engineering diagnostics only and are superseded for registration because the source and protocol changed after the first R4 dry-run.
- **Fresh terminal verdict:** `NO-GO_R4_REGISTERED`.
- **Blocking evidence:** the clean fixture has a real/null tie at zero utility; the R4 margin certificate omits view-weight and null-utility ranges; transport seeds are identical deterministic repeats; Gate 0 can pass unresolved protocol items; the fair baseline is fixed rather than matched-trainable; stop-first-fail and reproduction eligibility are incomplete.
- **R5 gate order:** freeze an entirely resolved R5 protocol -> implement strict augmented-gap fixture and bounded null utilities -> add genuine seed-specific initialization and balanced null metrics -> add a matched local/no-column-competition trainable baseline with a common oracle-frozen readout -> close structural, stop-order, exact64, eligibility and launcher negative tests -> fresh full-suite validation -> new R5 dry-run -> new one-step smoke -> local three-seed registered preexperiment -> retained-allocation 4161 run and two fresh-process reproductions only if every prior gate passes.
- **Concurrency boundary:** at most four active child agents; descendants are forbidden. The present wave owns four non-overlapping lanes: R5 protocol, R5 fixture/math, matcher hardening, and runner gate specification.
- **Formal-data boundary:** real-data main experiments and ablations remain `HOLD`; the official entity-level annotation/access boundary is not yet closed. Allocation `4161` must remain retained and must never be cancelled.

## 2026-07-22 R6 dry-run terminal verdict

- [x] Integrated strict semantic validation and completed the frozen R6 source/hash record.
- [x] Verified the frozen tree with `327 passed`, scoped Ruff/format, py_compile, three fresh source observations, and three Gate-0 processes (`66/66` each).
- [x] Ran exactly one fresh CPU-only R6 dry-run in `artifacts/calibration/capes_ci_qptm_r6_dryrun_20260722_v1`; launcher return code was 0 and no training/formal data access occurred.
- [x] Performed a separate non-overwriting post-run audit and preserved summary, stdout, stderr, and launch metadata.
- [ ] R6 is **not eligible for smoke or training**: the post-run strict validator produced 10 structural-evidence errors, including a structural microcase order/schema mismatch. Terminal audit verdict is `FAIL_STRICT_POSTRUN_SEMANTIC_VALIDATION`.
- [ ] Next authorized action is an R7 protocol/source correction and new freeze. Do not modify or rerun the frozen R6 artifact, do not start smoke/registered training, and do not open formal data.
- Formal main experiments and ablations remain `HOLD`; no model or dataset download is currently required. Allocation `4161` remains retained and must not be cancelled or released.

## Completion Gate

## 2026-07-23 R16/R17 reproduction-authority continuation

- [x] Preserve R16's failed audit and isolate the exact cause: R14 evidence was compared to R15 ancestry rather than its own frozen R14 origin pin.
- [x] Preserve R17's pre-root Gate-0 failure: comparator routing was corrected, but its freeze record was incomplete and the R16 projection-hash semantic was still inconsistent in one Gate-0 check.
- [ ] Create R18 as the smallest new authority: complete freeze-record closure plus consistent R16 freeze-record-projection validation, then repeat independent audit before any two-child CPU reproduction.
- [ ] Keep real-data/model download, GPU/4161, Slurm, formal test, and formal claim paths locked until independent reproduction is valid.
- [x] R18 complete freeze-record closure passed its staged 29-check validator, but cannot be used because the active runner was not yet R18 at the freeze boundary.
- [ ] R19 must first migrate the active runner and tests to the new authority, then freeze and perform independent Gate-0/audit before any reproduction authorization.

## 2026-07-22 R6 audit-hardening reset

- **Current authority under construction:** Phase 3E, R6 preexperiment qualification (`in_progress`). R5 dry-run v1 is an immutable technical-negative artifact because post-run audit found registered split access before authorization and multiple Gate-0/Gate-1 false positives.
- R5 v1 performed no training and did not use the formal test, but its `DRY_RUN_VALIDATED_R5` status is ineligible. The directory now contains a non-overwriting `postrun_audit.json` that records the invalid verdict and original summary SHA.
- R5 source corrections after the first dry-run require a new protocol ID and output root. R6 must close environment, implementation-vs-registry, initialization-hash, structural microcase, full-chain counterfactual, provenance, strict-schema, canonical-reproduction, atomic-failure and closed-source-allowlist evidence before any new dry-run.
- R6 structural/counterfactual/runner/reproduction lanes are integrated. The remaining pre-freeze lane is pure semantic recomputation of stored metric evidence; descendants remain forbidden and active children stay below the user limit of four. The next executable step remains a fresh R6 dry-run, not smoke or registered training.
- [x] Close staged access, output topology, source allowlist, atomic failure and stopped-child validation boundaries.
- [x] Persist raw evidence for transport/null/query/readout/marginal/binding/local metrics.
- [ ] Integrate pure semantic metric validation and rerun the complete suite.
- [ ] Freeze final observation/source/protocol records only after all source changes stop.
- [ ] Run one fresh R6 dry-run and perform a separate post-run audit; smoke remains locked until that audit passes.
- Real-data main/ablation remains `HOLD`; allocation `4161` remains retained and must never be cancelled.

## 2026-07-23 R19/R20 independent-reproduction continuation

- [x] Freeze native R19 runner/authority together and pass three independent Gate-0 processes at `74/74`.
- [x] Preserve the passing R19 audit and two child certificates as ineligible forensic evidence because they were issued manually before the launcher-owned transaction.
- [x] Freeze R20 with the launcher as the sole issuer-transaction owner. Final protocol SHA is `db7582b7fc25b8edfc8d693c046e62a09a014a31be54d7b3ed00df616ebc006d`; full registry SHA is `2a272c1dc90a22e2118f84cc6b3606709570452181fb9d785594940a37e1df6b`.
- [x] Pass three independent R20 Gate-0 processes at `74/74` with identical protocol, registry, source-manifest, and implementation-observation hashes.
- [x] Preserve the terminal R20 technical failure without retry: process A completed all eight compute gates, then the strict summary validator rejected its legal reproduction receipt because two post-root consumers read the wrong materializer-ID key. Process B never started.
- [x] Build R21 as the smallest new authority with one canonical `issuing_materializer_id` consumed identically by issuer, launcher, preclaim, summary receipt, and prepublication validation; prove the full behavior chain with isolated real validators/native claim transactions before freezing.
- [x] Freeze R21 after two independent red-team VETOs were repaired. Final protocol SHA is `693e9e887b9912fa00b532535be95e34abe41b0d15930505a457f8a781b92f1d`; full registry SHA is `a3735022a60575477800f0395f83dc7809a11c26a59dd41a36d363939e70f04f`; three independent Gate-0 processes passed `74/74`.
- [x] Preserve the terminal R21 technical failure without retry: process A completed all 500 steps and Gates 0--7, but the parent launcher stopped before process B because a process-local import observation was incorrectly included in the cross-process source-authority hash.
- [ ] Build R22 as an engineering-only authority correction. Separate the invariant hash over source schema/allowlist/files from `observed_workspace_imports`, add an unmocked child-to-launcher cross-process regression, freeze fresh R22 namespaces, and pass independent Gate-0.
- [ ] Execute one fresh R22 two-child CPU reproduction only after R22 is frozen and independently validated. Process B may start only after process A is strictly eligible.
- [ ] Keep download, formal-data, formal-test, GPU, and Slurm execution locked until R22 independent reproduction is terminally green.

## 2026-07-24 R22 terminal result / R23 required

- [x] Build and freeze R22 with the process-invariant source authority, unmocked
  cross-process eligibility regression, `192/192` focused tests, and three
  independent `74/74` Gate-0 passes.
- [x] Execute the one fresh R22 two-child CPU transaction without retry. Both
  children completed 500 steps for seeds `[17,29,43]` and persisted all eight
  compute-gate results.
- [x] Preserve R22 as an immutable technical failure. The parent canonical
  comparator emitted `independent_process_pids`, but the strict launcher's exact
  expected-check set omitted that key and rejected at `canonical_compare`.
- [ ] Build R23 as a one-field execution-control correction: add
  `independent_process_pids` to the validator's exact expected-check set; add
  acceptance plus missing/extra/false mutation regressions; change no scientific
  configuration, threshold, seed, step count, budget, or gate order.
- [ ] Freeze fresh R23 protocol/registry/source authority, pass all relevant tests
  and independent Gate-0 checks, then execute one new no-retry two-child CPU
  transaction under fresh namespaces.
- [ ] Keep model/data download, formal-data access, GPU/Slurm work, and allocation
  `4161` execution locked until R23 independent reproduction is terminally green.

### R23 live status

- [x] R23 exact comparator-key contract implemented and tested.
- [x] Complete relevant suite `196/196`, static checks, and isolated candidate
  byte-reproduction passed.
- [x] R23 frozen with `29/29` checks and three independent `74/74` Gate-0 passes.
- [x] R23 no-retry double-CPU reproduction completed both strictly eligible
  children, but the parent certificate failed closed on exactly one canonical
  mismatch:
  `/provenance/output_root_entry_evidence/output_root_contract/expected_leaf`.
- [ ] Build R24 as a one-pointer execution-control correction. Add only that
  process-specific leaf field to the post-eligibility canonical-comparison
  exclusions; preserve every scientific setting, seed, step, threshold, budget,
  gate, and child-eligibility rule.
- [ ] Prove the R24 comparator against preserved R23 A/B summaries, freeze fresh
  R24 namespaces, pass the complete relevant suite and three independent Gate-0
  processes, then execute one new no-retry two-child CPU transaction.
- [ ] Keep real model/data access and GPU/Slurm execution locked until the R24
  parent certificate is terminally green.

### R24 live status

- [x] PRE_FREEZE authority materialized from exact frozen R23 bytes and full
  registry; direct reproduction-contract diff is exactly one list entry.
- [x] Preserved-R23 positive/negative comparator tests, complete relevant suite
  (`253/253`), Ruff, format, py_compile, and isolated byte reproduction passed.
- [x] One-shot R24 finalizer passed `29/29`; three independent Gate-0 processes
  passed `74/74` with identical authority hashes.
- [x] Execute the fresh no-retry R24 sequential double-CPU transaction with
  process B gated behind process A strict eligibility.
- [x] R24 double-CPU transaction completed terminally green: both children
  passed `33/33` eligibility checks; the parent passed `11/11` checks with
  zero mismatches and exact canonical SHA-256.

## 2026-07-24 user-authorized real-data continuation

- [x] Complete a local mathematical-structure audit covering identifiability,
  two-sided null transport, global allocation, exact-token injection,
  longitudinal patient/time binding, leakage controls, and paired inference.
- [ ] Finish R23 and require terminally green independent reproduction; the user
  authorization does not waive this survival gate.
- [x] Inventory local real datasets/models read-only, validate license/DUA
  boundaries,
  checksums, patient/time fields, labels, split lineage, and storage boundary.
- [ ] Run the smallest real-data qualification smoke first. Scale to the retained
  `4161 / tpami / gpu01` allocation only after the real-data survival gate passes.
- [ ] Record every real run with command, config, hashes, environment, logs,
  outputs, metrics, failure cases, and gate verdict.

### Real qualification live status

- [x] Freeze the CheXTemporal-MIMIC matcher qualification protocol with exact
  R24 prerequisite, asset, source, model, coordinate, cohort, metric, bootstrap,
  stop-order, and interpretation boundaries.
- [x] Require the runner to validate exact R24 protocol/certificate/launcher
  hashes and terminal green fields before creating a real output root; focused
  real suite passes `12/12`.
- [x] Strict-load the frozen local BiomedCLIP checkpoint and verify GPU1 is free.
- [x] Preserve v1 as a no-retry technical-negative artifact: process A stopped
  before Q5 because cross-temporal local source IDs were not jointly unique;
  process B never started.
- [x] Build/freeze v2 with only the necessary real-structure corrections:
  unique joint source IDs plus true empty-side tensors for birth/death rows.
  The exact 267-row cached-feature replay passes all gates and the 10,000
  patient bootstrap.
- [x] Preserve v2 as a no-retry Q2 scientific stop: all other Q0/Q1/Q3/Q4/Q5
  gates passed, but the repeat check compared different batch shapes.
- [x] Freeze v3 with the exact-zero repeat criterion unchanged and the repeat
  forward fixed to the same registered batch shape; 680-crop GPU diagnostic is
  exact zero.
- [x] Run fresh v3 process A on GPU1. Start process B only if A passes Q0--Q5.
  Then issue the independent Q6 certificate; no retry or threshold/cohort
  search.
- [x] v3 completed terminally green without retry: both fresh processes passed
  Q0--Q5, the independent Q6 certificate passed all `19/19` checks, all output
  hashes matched exactly, and formal/clinical claim flags remained false.
- [ ] Freeze the first paper-grade real-data experiment only after reconciling
  the CAPES entity-level estimand with the available annotation unit. The v3
  matcher qualification does not by itself identify the five-label
  entity-level B4 causal claim.
- [ ] Inspect retained allocation `4161`, sync only the frozen experiment
  payload, and execute formal child steps without cancelling or releasing the
  parent allocation.

### First registered real-data secondary experiment

- [x] Freeze
  `docs/superpowers/specs/2026-07-24-chextemporal-chexpert-progression-pilot-v1.md`
  before execution, including exact assets/model hashes, cohort exclusions,
  five patient folds, systems, seeds, crossed derangements, optimization budget,
  bootstrap, stop order, and interpretation boundary.
- [x] Run the real CheXTemporal + CheXpert image experiment once on a free local
  RTX 3090. Retain 601 rows / 357 temporal pairs / 70 patients / 475 unique
  images; strict persistent B4 retains 90 rows / 22 patients.
- [x] Complete 25 independent result checks and persist `verification.json`
  with status `PASS_INDEPENDENT_RESULT_AUDIT`; summary SHA is
  `4afe60423ef1899063fd64a35ecc6ded05695c33d13fc170a4a04cfc02d8378d`
  and verifier certificate SHA is
  `a020f9938d5a458e6d85579ddc4da22c59574fe310b4a21c2df433e12acf2b1a`.
- [x] Record the full five-label structural upper-bound result: oracle-region
  patient-balanced macro-F1 `0.6907` versus paired-global `0.3262`, difference
  `+36.45 pp`, 95% CI `[+25.75, +48.65] pp`.
- [x] Apply the registered identity-specific stop rule: B4b-oracle minus
  B4a-deranged is `+7.25 pp`, but 95% CI `[-0.47, +16.92] pp`; B4b also does not
  beat paired-global. This does not establish the formal CAPES identity claim.
- [ ] Build the next dataset qualification around a larger same-entity
  persistent subset. Chest ImaGenome is the first authorized candidate;
  MS-CXR-T remains blocked until its project DUA is personally signed by the
  account holder.
- [ ] Only after the enlarged B4 survival gate is positive, sync the frozen
  payload to retained Slurm allocation `4161` for VLM transfer and formal
  ablations. Never cancel or release parent allocation `4161`.

## Session 4 (2026-07-25): R25 qualification runner + pytest fixes

- [x] Implement R25 qualification runner
  (`scripts/run_chest_imagenome_mimic_matcher_qualification.py`): validates
  R24 prerequisites, builds three-label cohort, evaluates Q0–Q7 gates.
- [x] Create Q6 fresh-process verifier
  (`scripts/verify_chest_imagenome_mimic_matcher_reproduction.py`).
- [x] Create R25 qualification tests
  (`tests/test_chest_imagenome_mimic_matcher_qualification.py`, 36 tests).
- [x] Extend `src/visualvit/real_qualification.py` with three-label helpers
  (additive: `THREE_LABEL_COMPARISON_MAP`, `three_label_from_comparison`,
  `persistent_label_coverage`).
- [x] Freeze R25 protocol (`refine-logs/CALIBRATION_PROTOCOL_R25_2026-07-25.md`)
  with correct base_dependency (R24), protocol_id, authority_state,
  status_vocabulary, allowlist, and recomputed freeze_record.
- [x] Fix pytest failure #1: mark
  `test_frozen_r14_validation_bundle_cannot_observe_live_r24_rules` as
  `xfail(strict=True)` — R14 bundle bytes unrecoverable.
- [x] Fix pytest failure #2: align `dry_run_authorized` check in
  `test_r6_resolution_and_nested_manifests_are_exact_and_fail_closed` to
  `is False` (matching production semantics).
- [x] Update `tests/test_frozen_source_manifest_cross_process.py` to copy
  R24 base protocol to isolated workspaces.
- [x] Verify: 479 passed + 1 xfailed; ruff clean; py_compile clean; R24 base
  integrity intact.

### Next steps
- [ ] Execute R25 qualification dry-run on Chest ImaGenome data (requires
  MIMIC-CXR metadata + image access at `F:\VisualVIT_runtime\050_routeC\data`).
- [ ] If Q0–Q7 gates pass, run two independent processes and invoke the Q6
  verifier to certify reproduction.
- [ ] If B4 survival gate is positive, sync frozen payload to allocation
  `4161` for VLM transfer and formal ablations.

## Completion Gate
只有在 Phase 5 审计完成、正式实验清单无未解释缺口、权威文档已写回且证据路径可用时，才可声明项目完成。
